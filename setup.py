"""

"""
from setuptools import setup, find_packages

setup(
    name = "deep_learning_model_zoo",
    version = "0.0",
    author = "qsx",
    packages = find_packages(),
    install_requires=["torch>=1.1", "torchvision", "numpy", "pytest", "pandas", "pyntcloud"]
)